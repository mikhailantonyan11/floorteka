## Инструкция по запуску и публикации

### 1. Запуск сайта у себя (локально)
1) Откройте PowerShell.
2) Перейдите в папку проекта:
   ```
   cd "C:\Users\user\Desktop\САЙТ ФЛОРТЕКА"
   ```
3) Установите зависимости (делается один раз, если уже ставили — можно пропустить):
   ```
   npm install
   ```
4) Запустите сервер:
   ```
   npm start
   ```
5) Откройте в браузере:
   ```
   http://localhost:3000
   ```

Чтобы остановить сервер: в терминале нажмите `Ctrl + C`.

---

### 2. Обновление сайта для других пользователей (Render)
Render берёт обновления с GitHub. Поэтому нужно отправить изменения в GitHub.

1) В PowerShell, в папке проекта:
   ```
   cd "C:\Users\user\Desktop\САЙТ ФЛОРТЕКА"
   git add .
   git commit -m "update site"
   git push
   ```
2) Render автоматически запустит новый деплой.
   - Если авто‑деплой отключён:  
     Render → ваш сервис → **Manual Deploy → Deploy latest commit**.
3) После статуса **Live** сайт обновится по той же ссылке.

---

### 3. Где взять ссылку на сайт
Render → ваш сервис → **Service URL** (вид `https://...onrender.com`).
Эту ссылку можно открыть на любом компьютере.

---

### 4. Как менять названия изображений
1) Все картинки должны лежать в папке:
   ```
   C:\Users\user\Desktop\САЙТ ФЛОРТЕКА\public\images
   ```
2) Название файла должно совпадать с полем `photo` в файле:
   ```
   C:\Users\user\Desktop\САЙТ ФЛОРТЕКА\data\stick.json
   ```
   Пример:
   - в `stick.json` стоит `"photo": "oxdog-gr.png"`
   - значит в `public\images` должен быть файл `oxdog-gr.png`
3) Как быстро скопировать и переименовать картинку:
   - Откройте папку с исходными файлами (Win + R → вставить → Enter):
     ```
     C:\Users\user\.cursor\projects\c-Users-user-Desktop\assets
     ```
   - Скопируйте нужную картинку в `public\images`
   - Переименуйте её точно под значение `photo`
4) После любых изменений с изображениями:
   - обновить поле `"photo"` в `stick.json`
   - сделать `git add .` → `git commit` → `git push`, чтобы обновилось на Render

---

### 5. Если `npm` или `node` не работает
Установите Node.js LTS: https://nodejs.org  
После установки перезапустите терминал и проверьте:
```
node -v
npm -v
```

---

### 6. Если `git` не работает
Установите Git для Windows: https://git-scm.com/downloads  
После установки перезапустите терминал и проверьте:
```
git --version
```
